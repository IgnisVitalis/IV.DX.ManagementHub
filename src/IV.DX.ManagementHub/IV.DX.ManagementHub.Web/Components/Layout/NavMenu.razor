@using IV.DX.ManagementHub.Web.Components.Custom
@using IV.DX.ManagementHub.Web.Components.Custom.Base
@inherits ManagementHubComponentBase

<nav class="sitenav" aria-labelledby="main-menu">
    <FluentNavMenu Id="main-menu" Collapsible="true" Width="250" Title="Navigation menu" @bind-Expanded="expanded">
        <FluentNavLink Href="/app/instances" Match="NavLinkMatch.Prefix">Instances</FluentNavLink>
        @if (!string.IsNullOrEmpty(AppKey) && roots is not null)
        {
            @foreach (var root in roots)
            {
                <DXNavMenuNode Node="root"
                               ExpandedState="new Dictionary<Guid, bool>()"
                               ComponentHrefFactory="@((type, id) => GetComponentHref(type, id))"
                               @key="root.Id" />
            }
        }
    </FluentNavMenu>
</nav>

@code {
    private bool expanded = true;
}
