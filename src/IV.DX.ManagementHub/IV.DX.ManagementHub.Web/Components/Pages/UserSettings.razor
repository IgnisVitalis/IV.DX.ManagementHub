@page "/app/{AppKey}/user"

@using IV.DX.ManagementHub.Web.Components.Custom.Base
@using IV.DX.ManagementHub.Web.Services

@inherits ManagementHubComponentBase

@inject AppAuthState AuthState
@inject NavigationManager NavigationManager

<PageTitle>Super Admin Settings</PageTitle>

<FluentCard Class="user-settings-card">
    <FluentStack Orientation="Orientation.Vertical" Width="100%" VerticalGap="12">
        <FluentStack Orientation="Orientation.Horizontal"
                     Width="100%"
                     HorizontalAlignment="HorizontalAlignment.SpaceBetween"
                     VerticalAlignment="VerticalAlignment.Center">
            <FluentLabel Typo="Typography.Subject">Super Admin Settings</FluentLabel>

            <FluentButton Appearance="Appearance.Stealth"
                          Title="Back to home"
                          aria-label="Back to home"
                          OnClick="GoToHome">
                <FluentIcon Value="@(new Icons.Regular.Size16.Dismiss())" />
            </FluentButton>
        </FluentStack>

        <FluentDivider />

        <div class="user-settings-grid">
            <FluentLabel Color="Color.Disabled" Weight="FontWeight.Bold">Username</FluentLabel>
            <FluentLabel>@DisplayUserName</FluentLabel>

            <FluentLabel Color="Color.Disabled" Weight="FontWeight.Bold">Role</FluentLabel>
            <FluentLabel>Root</FluentLabel>

            <FluentLabel Color="Color.Disabled" Weight="FontWeight.Bold">Scope</FluentLabel>
            <FluentLabel>Global</FluentLabel>

            <FluentLabel Color="Color.Disabled" Weight="FontWeight.Bold">Session</FluentLabel>
            <FluentLabel>@SessionStatus</FluentLabel>

            <FluentLabel Color="Color.Disabled" Weight="FontWeight.Bold">Token Preview</FluentLabel>
            <FluentLabel>@TokenPreview</FluentLabel>
        </div>

        <FluentDivider />

        <FluentStack Orientation="Orientation.Horizontal" HorizontalGap="8">
            <FluentButton Appearance="Appearance.Outline" OnClick="SignOut">Sign out</FluentButton>
        </FluentStack>
    </FluentStack>
</FluentCard>

@code
{
    private string DisplayUserName =>
        string.IsNullOrWhiteSpace(AuthState.UserName) ? "root" : AuthState.UserName;

    private string SessionStatus => AuthState.IsAuthenticated ? "Authenticated" : "Not authenticated";

    private string TokenPreview
    {
        get
        {
            if (string.IsNullOrWhiteSpace(AuthState.AccessToken))
            {
                return "Not available";
            }

            var token = AuthState.AccessToken;
            var length = Math.Min(20, token.Length);
            return $"{token[..length]}...";
        }
    }

    private void GoToHome()
    {
        NavigationManager.NavigateTo($"/app/{AppKey}/instances");
    }

    private void SignOut()
    {
        AuthState.Clear();
        NavigationManager.NavigateTo("/", replace: true);
    }
}
