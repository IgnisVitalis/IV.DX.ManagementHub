@page "/"
@using IV.ManagementHub.Web.Services
@inject NavigationManager NavigationManager

@code
{
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/app/instances", replace: true);
    }
}
