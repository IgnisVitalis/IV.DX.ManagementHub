@using IV.ManagementHub.Web.Components.Custom.Base
@using Microsoft.FluentUI.AspNetCore.Components
@using Newtonsoft.Json.Linq
@using System.Linq
@using IV.DX.Kernel.Enums;
@using System;

@if (Definition == null)
{
    <FluentProgressRing />
}
else
{
    <FluentStack Orientation="Orientation.Vertical" Width="100%">
        <FluentLabel Typo="Typography.Subject">@Definition.Name</FluentLabel>
        <FluentDivider></FluentDivider>
        <FluentCard Class="unclip">
            <FluentToolbar id="@Definition.Name" style="width:100%">
                <FluentButton aria-label="Add item" IconEnd="@(new Icons.Regular.Size16.AddCircle())" OnClick="Add" />
            </FluentToolbar>
            <FluentDataGrid id="@Definition.Name"
                            Items="@(this.DXMultiElement.GetAnnounced(true).AsQueryable())"
                            TGridItem="IV.DX.Kernel.Models.DXItem"
                            RowSize="@DataGridRowSize.Medium">
                @foreach (var column in Definition.Columns)
                {
                    <TemplateColumn Title="@column.Name" Align="Align.Center" Style="overflow:visible">
                        <DXItemEditor ColumnDefinition="column" DXItem="context"></DXItemEditor>
                    </TemplateColumn>                 
                }
                <TemplateColumn Title="Actions" Align="@Align.Center" Width="10rem">
                    <FluentButton OnClick="@(() => Remove(context))">
                        <FluentIcon Value="@(new Icons.Regular.Size16.Delete())" Color="Color.Error" />
                    </FluentButton>
                </TemplateColumn>
            </FluentDataGrid>
        </FluentCard>
    </FluentStack>
}